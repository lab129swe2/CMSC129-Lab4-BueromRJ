# Copy these values into `.env` only while running Firebase emulator-backed integration tests.
FIREBASE_PROJECT_ID=task-manager-local
FIRESTORE_EMULATOR_HOST=127.0.0.1:8081
FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099
FIREBASE_EMULATOR_UI_HOST=127.0.0.1:4000

BACKEND_PORT=3001
FRONTEND_PORT=5173
CORS_ORIGIN=http://localhost:5173
