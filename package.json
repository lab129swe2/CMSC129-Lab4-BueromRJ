{
  "name": "cmsc129-lab4-task-manager",
  "private": true,
  "scripts": {
    "emulators:start": "firebase emulators:start --only auth,firestore,ui",
    "emulators:exec:integration": "firebase emulators:exec --only auth,firestore \"npm --prefix backend run test:integration\""
  },
  "devDependencies": {
    "firebase-tools": "^14.11.0"
  }
}
